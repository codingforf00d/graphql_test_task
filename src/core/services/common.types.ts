export interface AuthorsIds {
  authorsIds: ReadonlyArray<number>;
}

export interface BooksIds {
  booksIds: ReadonlyArray<number>;
}

export interface CountriesIds {
  countriesIds: ReadonlyArray<number>;
}

export interface PublishersIds {
  publishersIds: ReadonlyArray<number>;
}

export interface CategoriesIds {
  categoriesIds: ReadonlyArray<number>;
}
